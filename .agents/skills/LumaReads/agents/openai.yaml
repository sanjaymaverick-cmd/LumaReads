interface:
  display_name: "LumaReads"
  short_description: "Repo-specific patterns and workflows for LumaReads"
  default_prompt: "Use the LumaReads repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true